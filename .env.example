MONGODB_URI="mongodb+srv://<username>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority"
MONGODB_DB="<optional-db-name-if-not-in-uri>"

# Admin access key (server-side recommended).
ADMIN_KEY="123456"
